"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const execa_1 = __importDefault(require("execa"));
function previewFeatures(execPath, dataSourceOnly, onError) {
    let result;
    if (dataSourceOnly) {
        result = execa_1.default.sync(execPath, ['preview-features', '--datasource-only']);
    }
    else {
        result = execa_1.default.sync(execPath, ['preview-features']);
    }
    if (result.exitCode !== 0) {
        const errorMessage = "prisma-fmt error'd during getting available preview features.\n";
        if (onError) {
            onError(errorMessage + result.stderr);
        }
        console.error(errorMessage);
        console.error(result.stderr);
        return [];
    }
    return JSON.parse(result.stdout);
}
exports.default = previewFeatures;
//# sourceMappingURL=previewFeatures.js.map