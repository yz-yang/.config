/**
 * Imports
 */
/**
 * Install prisma format
 */
export declare function plusXSync(file: string): void;
export declare type DownloadResult = {
    lastModified: string;
    sha256: string;
    zippedSha256: string;
};
export declare function downloadZip(url: string, target: string): Promise<DownloadResult>;
export default function install(url: string, fmtPath: string): Promise<string>;
